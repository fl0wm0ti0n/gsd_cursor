# Sprint S0042 Progress

- Implemented installer manifest updates for `its_magic/`.
- Updated PowerShell/shell/Python installers to use canonical
  `its_magic/.its-magic-version` with legacy fallback migration.
- Added installer metadata README surfaces in active/template trees.
- Updated CLI/help, runbook, README, and regression tests.
