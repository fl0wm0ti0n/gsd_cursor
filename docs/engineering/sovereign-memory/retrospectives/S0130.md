# Curator Retrospective — S0130 / US-0130 (Operator-pinned sovereign-critic model)

```json
{
  "sprint_id": "S0130",
  "story_ids": ["US-0130"],
  "release_ref": "handoffs/releases/S0130-release-notes.md",
  "summary": "US-0130 shipped MODEL_SOVEREIGN-CRITIC pin + optional catalog roles.critic overlay on select_critic_model, CATALOG_OPTIONAL_ROLE_KEYS, cursor_only 9th example, 10 contract-test markers; no companion DEC; lifecycle closed through refresh-context.",
  "learnings": [
    "Critic pin is an overlay inside select_critic_model, not a canonical-phase 5-step chain row: MODEL_SOVEREIGN-CRITIC (hyphen exact) > roles.critic when role_catalog > US-0104 opposition UNCHANGED.",
    "critic stays optional: CATALOG_OPTIONAL_ROLE_KEYS={\"critic\"}; do not add critic to required CATALOG_ROLE_KEYS or PHASE_LOGICAL_ROLE.",
    "Same-slug collision keeps CROSS_MODEL_DEGRADED_MODE; underscore alias MODEL_SOVEREIGN_CRITIC must not be consumed.",
    "No companion DEC: US-0130 composes DEC-0104 §5 / DEC-0087 / DEC-0086; a new DEC-0130 would duplicate governance. Never write model-catalog.local.json."
  ],
  "promoted_entry_ids": []
}
```

- **Story**: US-0130 (Operator-pinned sovereign-critic model via catalog `roles.critic` and `MODEL_SOVEREIGN-CRITIC`)
- **Sprint**: S0130 (RELEASED — lifecycle terminal at refresh-context 2026-08-26T22:54:00Z)
- **Orchestrator run**: `auto-20260826-01`
- **Verdict**: PASS (segment_closed=true; 9/9 ACs; 10/10 contract tests; 9/9 compose guards UNCHANGED)
- **Curator runtime proof**: `rp-auto-20260826-01-refresh-context-curator-20260826T225400Z-US-0130`
- **Curator fresh_context_marker**: `cur-US0130-refresh-context-20260826T225400Z-fresh`

## Lifecycle summary

US-0130 fully closed: spec (intake RE-ATTEST + discovery) → research (R-0112 DQ1–DQ8) → architecture (no companion DEC) → sprint-plan → execute → qa → verify-work → release (1st attempt) → closure → sovereign-critic → refresh-context.

Final state: S0130 RELEASED; US-0130 DONE; acceptance L158 ticked; harness Pass:845/Fail:0 @ 2026-08-26T22:41:33Z.

## Key patterns

1. **Overlay, not synthetic phase**: pin/catalog overlay prepends `select_critic_model`; opposition `_resolve_slug_for_tier` stays for fallback.
2. **Optional critic allowlist**: missing `roles.critic` is valid; empty-present reuses `MODEL_CATALOG_SCHEMA_V2_INVALID`.
3. **Installer compose**: ship cursor_only as 9th example with `critic=composer-2.5-fast`; never write operator `model-catalog.local.json`.

## Drain state

- **next_eligible_open_story**: orchestrator-owned (OPEN remain: US-0129 P2)
- **drain_advance_action**: orchestrator-owned (curator STOP; do not select/start)

## Triad rollover

Pre-append: `--rollover` then `--check` around state.md refresh-context append; verification tuple recorded in `docs/engineering/state.md`.
