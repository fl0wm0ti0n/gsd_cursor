# Curator Retrospective — S0128 / US-0128 (Convergence smoke surrogate for waived-probe UAT slices)

```json
{
  "sprint_id": "S0128",
  "story_ids": ["US-0128"],
  "release_ref": "handoffs/releases/S0128-release-notes.md",
  "summary": "US-0128 shipped _eval_smoke_green waived-probe surrogate, canonical convergence_smoke uat step, CONVERGENCE_SMOKE_SURROGATE_MISSING, qa/verify-work contracts, and 11 contract-test markers; lifecycle closed through refresh-context.",
  "learnings": [
    "smoke_green needs a documented surrogate for ultra_lean/docs slices: waived live-runtime probes plus green harness must PASS without a fake browser step.",
    "Legacy _uat_smoke_passes stays first; surrogate is an additional PASS path inside the same conjunct — do not renumber DEC-0110 five-conjunct structure.",
    "CONVERGENCE_SMOKE_SURROGATE_MISSING is for missing surrogate prerequisites; CONVERGENCE_SMOKE_PROBE_FAIL stays for real smoke-step / deploy-smoke failures.",
    "No companion DEC: US-0128 is an additive surrogate branch under DEC-0110 §10 / DEC-0078; a new DEC would duplicate governance."
  ],
  "promoted_entry_ids": []
}
```

- **Story**: US-0128 (Convergence smoke surrogate for contract-test and waived-probe UAT slices)
- **Sprint**: S0128 (RELEASED — lifecycle terminal at refresh-context 2026-08-26T21:12:00Z)
- **Orchestrator run**: `auto-20260826-01`
- **Verdict**: PASS (segment_closed=true; 6/6 ACs; 11/11 contract tests; 8/8 compose guards UNCHANGED)
- **Curator runtime proof**: `rp-auto-20260826-01-refresh-context-curator-20260826T211200Z-US-0128`
- **Curator fresh_context_marker**: `cur-US0128-refresh-context-20260826T211200Z-fresh`

## Lifecycle summary

US-0128 fully closed: spec → research (R-0111 DQ1–DQ8) → architecture (no companion DEC) → sprint-plan → execute → qa → verify-work → release (1st attempt) → closure → sovereign-critic → refresh-context.

Final state: S0128 RELEASED; US-0128 DONE; acceptance L156 ticked; harness Pass:845/Fail:0 @ 2026-08-26T20:57:42Z.

## Key patterns

1. **Legacy-first smoke_green**: `_eval_smoke_green` keeps `_uat_smoke_passes`; surrogate activates only when no real smoke step exists.
2. **Six-class waived inventory**: `browser_smoke`, `api_health`, `process_health`, `cli_smoke`, `build`, `manual_operator` with `UAT_PROBE_FORBIDDEN`; `test`/`contract_tests_primary` is never waived.
3. **Canonical emission**: `/qa` and `/verify-work` emit `id=convergence_smoke` when `contract_test_failed=0`.

## Drain state

- `next_eligible_open_story`: orchestrator-owned (OPEN remain: US-0130 P1, US-0129 P2)
- `drain_advance_action`: orchestrator-owned (curator STOP; do not select/start)

## Triad rollover

Pre-append: `--rollover` then `--check` around state.md refresh-context append; verification tuple recorded in `docs/engineering/state.md`.
