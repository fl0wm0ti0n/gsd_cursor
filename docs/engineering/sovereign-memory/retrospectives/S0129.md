# Curator Retrospective — S0129 / US-0129 (Architecture hot-surface rollover linkage guard)

```json
{
  "sprint_id": "S0129",
  "story_ids": ["US-0129"],
  "release_ref": "handoffs/releases/S0129-release-notes.md",
  "summary": "US-0129 shipped scripts/arch_linkage_guard.py pre+post wrap of triad --rollover, fail-closed ARCH_LINKAGE_ROLLOVER_BLOCKED (security_hard, never skip), default-off ARCH_LINKAGE_AUTO_REPAIR=0 (not in AUTONOMY_PRESET), stdlib heading discovery, DQ8 H1 stub before US-0089/US-0090 tail, 8 contract-test markers, harness 26AB; lifecycle closed through refresh-context.",
  "learnings": [
    "Wrap --rollover with pre-block (no partial archive write) and post-verify; ARCH_LINKAGE_ROLLOVER_BLOCKED is security_hard and must never skip.",
    "Discover required architecture H1s from live contract tests (stdlib), not a hand-maintained manifest — that drift is the US-0126 B-1 class.",
    "ARCH_LINKAGE_AUTO_REPAIR stays default-off and off AUTONOMY_PRESET; bounded H1 stub + pack_ref is the repair shape, not full-section restore.",
    "Companion DEC-0129 is required: new fail-closed family + security_hard row + scratchpad flag + stub insertion contract. DEC-0054 owns archiver split/caps, not linkage."
  ],
  "promoted_entry_ids": []
}
```

- **Story**: US-0129 (Architecture hot-surface rollover linkage guard — active contract preservation)
- **Sprint**: S0129 (RELEASED — lifecycle terminal at refresh-context 2026-08-27T09:04:03Z)
- **Orchestrator run**: `auto-20260827-01`
- **Verdict**: PASS (segment_closed=true; 6/6 ACs; 8/8 contract tests; 8/8 compose guards UNCHANGED)
- **Curator runtime proof**: `rp-auto-20260827-01-refresh-context-curator-20260827T090403Z-US-0129`
- **Curator fresh_context_marker**: `cur-US0129-refresh-context-20260827T090403Z-fresh`

## Lifecycle summary

US-0129 fully closed: spec (intake RE-ATTEST + discovery) → research (R-0113 DQ1–DQ8) → architecture (DEC-0129) → sprint-plan → execute → qa → verify-work → release (1st attempt) → closure → sovereign-critic → refresh-context.

Final state: S0129 RELEASED; US-0129 DONE; acceptance L157 ticked; harness Pass:847/Fail:0 @ 2026-08-27T08:41:43Z.

## Key patterns

1. **Pre-block then post-verify**: predicted required-heading pop with flag off emits `ARCH_LINKAGE_ROLLOVER_BLOCKED` and does not write packs or the hot file.
2. **Consumer-driven headings**: `discover_required_arch_headings` scans live architecture.md asserts in `tests/**/*_test.py` (exclude `tests/.tmp*`).
3. **Default-off repair**: `ARCH_LINKAGE_AUTO_REPAIR=0`; stub is `# US-xxxx — <archive title>` plus one-line pack_ref before the US-0089/US-0090 tail.

## Drain state

- **next_eligible_open_story**: orchestrator-owned (OPEN remain: none — curator did NOT select/start)
- **drain_advance_action**: not_applicable (curator STOP; do not drain-advance)

## Triad rollover

`scripts/triad_hygiene.py` absent — equivalent `python scripts/enforce-triad-hot-surface.py --rollover --check` with live `arch_linkage_guard.py` pre/post (US-0129 product). Verification tuple recorded in `docs/engineering/state.md`. `ARCH_LINKAGE_AUTO_REPAIR` stayed default-off.
