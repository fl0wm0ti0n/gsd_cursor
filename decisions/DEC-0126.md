# DEC-0126: OpenCode host operator runbook + consolidated cross-host reason-code table + `--scope=opencode-adapter` parity extension + 12 static/grep contract markers + locked operator sentences (US-0126)

- Date: 2026-08-25
- Status: Accepted
- Story: `US-0126`
- Research refs: `R-0109` (deepened 2026-08-25, tech-lead, `/research`, auto-20260825-01 — DQ1..DQ8 LOCKED for US-0126; US-0121 Q1..Q12 + US-0122 DQ1..DQ8 + US-0123 DQ1..DQ10 + US-0124 DQ1..DQ8 + US-0125 DQ1..DQ8 locks preserved)
- Composes on: **`US-0071`** (operator-sentence sanitization — no DEC ids in operator prose), **`US-0113`..`US-0117`** (operator docs — add OpenCode host section; do not rewrite Cursor command catalogs), **`US-0121` / `DEC-0120`** (installer `--host` flag docs hook — `## OpenCode host mode (US-0121)` h2 untouched; US-0126 cross-links), **`US-0122` / `DEC-0122`** (seven role agents — referenced, not redefined), **`US-0123`** (per-role slug routing — referenced, not re-listed), **`US-0124` / `DEC-0124`** (orchestrator plugin + stub reason-code h2 untouched — US-0126 owns consolidated table; cross-links), **`US-0125` / `DEC-0125`** (thin commands + validator-bridge stub h2 untouched — US-0126 owns consolidated table; **DEC-0125 DQ7 raw Python reason codes upheld — `OPENCODE_VALIDATOR_FAILED` wrapper NOT resurrected**), **`US-0102` / `DEC-0087`** (no vendor slugs in `template/`)
- Extends: `scripts/check_intake_template_parity.py` `OPENCODE_ADAPTER_PAIRS` (additive — 2 new pairs; no sibling script/scope); `docs/engineering/runbook.md` + `template/docs/engineering/runbook.md` (new sibling h2 — additive; active↔template byte-identical); `README.md` + `template/its_magic/README.md` (new user-visible OpenCode host blurb — additive); `tests/us0126_contract_test.py` + `template/tests/us0126_contract_test.py` (NEW — 12 markers, byte-identical pair)
- Related: `US-0121` (installer `--host` flag + h2 — cross-linked, untouched), `US-0122` (role agents — referenced), `US-0123` (slug routing — referenced), `US-0124` (plugin + stub h2 — cross-linked, untouched), `US-0125` (thin commands + stub h2 — cross-linked, untouched), `US-0071` (sanitization — enforced), `US-0102` (no vendor slugs — upheld)
- Supersedes: none

## Context

US-0126 is the sixth and final slice of the six-story OpenCode adapter epic (US-0121..US-0126). US-0121 shipped the empty-but-valid `template/.opencode/` pack + the `--host` installer switch. US-0122 populated the pack with eight markdown role agents and locked the Layer-1 permission matrix. US-0123 locked the per-role `provider/slug` resolution chain. US-0124 shipped the orchestrator plugin that makes `/auto` spawn-only on the OpenCode host. US-0125 shipped the 15 dispatch-only thin commands + the Python validator bridge contract. US-0126 owns **Layer 4** — the operator-facing runbook section, the consolidated cross-host reason-code table, the `--scope=opencode-adapter` parity extension, and the 12 `test_us0126_*` contract markers.

R-0109 DQ1..DQ8 deepened the eight discovery questions against the existing OpenCode-epic runbook h2 inventory, the existing `OPENCODE_ADAPTER_PAIRS` tuple, and the US-0124/US-0125 reason-code locks. This DEC locks the answers as the cross-cutting operator-runbook + reason-code-table + parity-extension + contract-test contract that execute inherits without re-deriving.

Three sovereign-critic non-blocking carry-forwards from the research phase are closed here:

1. **`ik_us0126_dq3_parity_grep_false_pass`** — DQ3 listed reason-code table presence + `test_us0126_*` markers as parity-CLI predicates, which would false-pass if the parity CLI only checks byte-identity. Closed by **DQ3 explicit layer split**: `--scope=opencode-adapter` = **byte-identical pair check only**; reason-code table presence + `test_us0126_*` markers = **contract-test grep** (in `tests/us0126_contract_test.py`), NOT parity-CLI predicates. Execute must not overload `check_intake_template_parity.py` with grep predicates.
2. **`ik_us0126_layering_runbook_dec_tests`** — the runbook pair is a whole-file byte-identical pair (installer-owned `docs`), which couples the new h2 section to every other runbook edit. Closed by documenting the **coupling risk** explicitly: execute must keep active↔template runbook byte-identical after adding the new h2 — any drift fails `--scope=opencode-adapter`. Intentional (the runbook is installer-owned).
3. **`ik_us0126_research_scope_yagni_markers`** — research floated 12 markers with a possible collapse to 11 (merging marker 4 + marker 12) and left the AC-10 cursor-docs baseline open. Closed by **DQ4**: 12 markers locked (AC-5 split is real — readme vs runbook no-dec-leak are distinct surfaces; marker 4 + marker 12 kept separate for explicit defense in depth); AC-10 baseline = **deterministic static check** vs current-kit-inventory (a sorted file-name list of `.cursor/commands/*.md` + `.cursor/agents/*.md` checked into the repo at execute time), NOT a frozen pre-US-0126 git snapshot (fragile) and NOT a hash manifest of the entire `.cursor/` directory (over-broad).

Research basis: **`R-0109`** US-0126 deepened findings (DQ1..DQ8 LOCKED for `/architecture`; prior epic-slice locks preserved, not wiped).

## Decision

### 1. Runbook section = new sibling h2 `## OpenCode host operator runbook (US-0126)` after US-0125 h2 (DQ1 LOCKED — AC-1)

Ship a new sibling h2 in `docs/engineering/runbook.md` + `template/docs/engineering/runbook.md` (byte-identical active↔template):

- **Heading**: `## OpenCode host operator runbook (US-0126)`
- **GitHub anchor**: `opencode-host-operator-runbook-us-0126`
- **Placement**: immediately after the `## OpenCode thin commands + validator bridge (US-0125)` section, before the next non-OpenCode h2.
- **Mirror**: `template/docs/engineering/runbook.md` (active↔template byte-identical parity — DQ8; validated by the new `docs/engineering/runbook.md` ↔ template pair in `OPENCODE_ADAPTER_PAIRS` — DQ3).
- **US-0121/US-0124/US-0125 h2 sections untouched** (compose, do not amend).

**Rejected alternatives**: (a) Merge into US-0121 installer-flag h2 — rejected as compose-do-not-amend violation (US-0121 h2 locked by US-0121 AC-9); (b) Generic `## OpenCode host` h2 (no US-xxxx suffix) — rejected as it breaks the `(US-xxxx)` suffix convention for the five sibling OpenCode-epic h2 sections.

### 2. Locked operator sentences (DQ5, DQ6, DQ7 LOCKED — AC-6, AC-7, AC-8)

**Program DoD sentence (DQ5)** — verbatim into the runbook h2 body:

> "Program done: with a fresh `its-magic --host opencode` install and `/connect`ed keys, an operator can run `intake → … → release` on stock OpenCode with PO/Dev/QA as distinct sessions (optionally distinct providers per US-0123 role-slug routing), and the Python persistence-blocking validators (`intake_evidence_validate.py`, `bug_issue_validate.py`, and the US-0125 bridge contract set) refuse writes on non-zero exit exactly as on the Cursor host."

- **"without Cursor"** = no `.cursor/` directory loaded for this project (installer `--host opencode` skips `.cursor/` rows per US-0121; kernel paths still install). NOT "no Cursor IDE process on the operator machine".
- **"different sessions/providers"** = distinct OpenCode sessions (PO/Dev/QA as separate `opencode run --session` invocations or separate TUI sessions per US-0069 / DEC-0051); optionally distinct `/connect` profiles or distinct provider slugs per role (US-0123).
- **"validators still block"** = the regression baseline is the existing Python validator set (`intake_evidence_validate.py`, `bug_issue_validate.py`, plus the US-0125 bridge contract) — Python SOT; the US-0124 plugin `ctx.tool.hook("execute.before")` enforces persistence on non-zero exit.
- Static documentation test (grep for key phrases); NOT a live end-to-end probe.

**Default-host reminder sentence (DQ6)** — verbatim into the runbook h2 body + README user-visible OpenCode host blurb:

> "Default install is cursor-only. Pass `--host opencode` or `--host both` to install the OpenCode host adapter; without it, `.opencode/` is not installed. See `## OpenCode host mode (US-0121)` for the installer flag reference."

- **No DEC ids** in the operator-facing sentence (US-0071). Cross-reference is to the US-0121 runbook h2 heading, not to `DEC-0120`.

**Out-of-scope list (DQ7)** — verbatim into the runbook h2 body + README user-visible OpenCode host blurb (operator prose only):

> "Out of scope for the OpenCode host adapter: standalone runtime, OpenCode fork, VS Code contrib rewrite, Caveman mode, Cursor browser as primary UAT."

- **Boundaries subsection** (separate, not operator prose; runbook only; cross-references allowed here):
  - "standalone runtime — see `docs/product/standalone-runtime-masterplan.md`."
  - "OpenCode fork — out of scope; the adapter uses stock OpenCode plugins/agents/commands only."
  - "VS Code contrib rewrite — out of scope; the adapter does not modify VS Code or its contrib extensions."
  - "Caveman mode — see `DEC-0055`."
  - "Cursor browser as primary UAT — out of scope; browser UAT remains a secondary surface (US-0093)."

**Rejected alternatives**: (a) DEC-id cross-references inline in operator prose — rejected as US-0071 violation; (b) No cross-references at all — rejected as operators cannot find owning masterplans/DECs; (c) Live end-to-end probe DoD — rejected as it adds OpenCode runtime dependency to CI (vision D10 violation).

### 3. Consolidated cross-host reason-code table (DQ2 LOCKED — AC-2; `OPENCODE_VALIDATOR_FAILED` wrapper NOT resurrected per DEC-0125 DQ7)

The consolidated table documents **four `OPENCODE_*` codes from US-0124** + **five installer `OPENCODE_*`/`CURSOR_*` codes from US-0121** + **three reused cross-host codes** + **raw Python validator codes (no wrapper)**. Each code has a one-line semantics + fail-closed action + cross-link to its owning slice. The table cross-links to the US-0124 stub h2 (`## OpenCode orchestrator plugin reason codes (US-0124)`) and US-0125 stub h2 (`## OpenCode thin commands + validator bridge (US-0125)`) for the per-slice stub references; US-0126 owns the consolidated cross-host view.

- **`OPENCODE_*` family (OpenCode-host-specific — from US-0124 / DEC-0124 DQ4)**: `OPENCODE_PLUGIN_SPAWN_UNSUPPORTED`, `OPENCODE_SUBTASK_IGNORED`, `OPENCODE_HEADLESS_UNSUPPORTED`, `OPENCODE_DRIVER_INVOKE_FAILED`.
- **Installer `OPENCODE_*` / `CURSOR_*` family (from US-0121 / DEC-0120)**: `INSTALL_HOST_INVALID`, `OPENCODE_ORPHANED_BY_CLEAN_CURSOR`, `OPENCODE_STALE_BY_UPGRADE_CURSOR`, `CURSOR_ORPHANED_BY_CLEAN_OPENCODE`, `CURSOR_STALE_BY_UPGRADE_OPENCODE`.
- **Reused cross-host codes (no `OPENCODE_` prefix — same semantics on Cursor + OpenCode)**: `AUTO_ORCHESTRATOR_PHASE_EXECUTION`, `PHASE_ROLE_MISMATCH`, `NATIVE_CHAIN_UNAVAILABLE`.
- **Raw Python validator reason codes (Python SOT — no `OPENCODE_*` wrapper per DEC-0125 DQ7)**: `INTAKE_PERSISTENCE_BLOCKED`, `INTAKE_REQUIRED_TOPIC_MISSING` (from `intake_evidence_validate.py`); `BUG_ISSUE_VALIDATION_FAILED` (from `bug_issue_validate.py`).

US-0126 documents these as the persistence-blocking gate surface; US-0125 owns the bridge contract; US-0126 does NOT enumerate every kit validator — the bridge contract is generic.

**Rejected alternatives**: (a) Resurrect `OPENCODE_VALIDATOR_FAILED: <python_code>` wrapper — rejected as DEC-0125 DQ7 REJECTED it (duplicates namespace, pre-empts US-0126 table); (b) Enumerate every kit validator — rejected as US-0125 owns the bridge contract (generic), not the validator inventory.

### 4. Parity scope = additive `OPENCODE_ADAPTER_PAIRS` extension (2 new pairs) + explicit layer split (DQ3 LOCKED — AC-3; critic NB `ik_us0126_dq3_parity_grep_false_pass` closed)

Extend `OPENCODE_ADAPTER_PAIRS` in `scripts/check_intake_template_parity.py` additively with 2 new pairs (no sibling script, no sibling scope):

- **Existing pairs preserved (8)**: `installer-owned-paths.manifest` ↔ template, `check_intake_template_parity.py` ↔ template, `tests/us0121_host_mode_test.py` ↔ template, `tests/us0122_contract_test.py` ↔ template, `tests/us0123_contract_test.py` ↔ template, `tests/us0124_contract_test.py` ↔ template, `tests/us0125_contract_test.py` ↔ template, `model_tier_validate.py` ↔ template.
- **New pairs added by US-0126 (2)**: `tests/us0126_contract_test.py` ↔ `template/tests/us0126_contract_test.py`; `docs/engineering/runbook.md` ↔ `template/docs/engineering/runbook.md`.

**Explicit layer split** (critic NB closed — document so execute does not overload `check_intake_template_parity.py`):

- **`--scope=opencode-adapter` parity CLI predicate** = **byte-identical pair check only**. Each pair must be byte-identical (file content hash match); each enumerated surface file must exist (non-empty). Exit 0 = `[INTAKE_TEMPLATE_PARITY_OK] scope=opencode-adapter`; non-zero = `INTAKE_TEMPLATE_PARITY_FAILED` with the failing pair name. The parity CLI does NOT grep for reason-code table presence and does NOT grep for `test_us0126_*` markers.
- **Reason-code table presence + `test_us0126_*` markers** = **contract-test grep**, NOT parity-CLI predicates. `test_us0126_reason_code_catalog_present` (marker 2) and `test_us0126_test_marker_checklist` (marker 4) / `test_us0126_prior_story_markers_present` (marker 12) are the grep layers — they live in `tests/us0126_contract_test.py`, not in `check_intake_template_parity.py`. Execute must NOT add reason-code-table or test-marker grep predicates to the parity CLI; the parity CLI stays byte-only.

**Rejected alternatives**: (a) New sibling script `scripts/check_opencode_adapter_parity.py` — rejected as it violates the established `--scope=<name>` pattern (15 scopes on the single CLI); (b) New `--scope=opencode-adapter-us0126` sibling scope — rejected as it fragments the epic parity surface; `--scope=opencode-adapter` was always intended to cover the whole epic (US-0121..US-0126).

### 5. `test_us0126_*` contract-test list = 12 markers, static/grep, no live OpenCode probe (DQ4 LOCKED — AC-4; critic NB `ik_us0126_research_scope_yagni_markers` closed)

`tests/us0126_contract_test.py` + `template/tests/us0126_contract_test.py` (byte-identical pair — DQ3). All markers are static/grep-based; no live OpenCode runtime probe (vision D10 lock). 12 markers (one-test-per-AC, AC-5 splits into readme + runbook no-dec-leak; +1 aggregate prior-story marker — kept separate from marker 4 for explicit defense in depth):

1. `test_us0126_runbook_section_present` (AC-1) — grep runbook + template for `## OpenCode host operator runbook (US-0126)` h2.
2. `test_us0126_reason_code_catalog_present` (AC-2) — grep runbook for each code in the consolidated table; assert each code has one-line semantics + fail-closed action.
3. `test_us0126_parity_scope_opencode_adapter` (AC-3) — run `python scripts/check_intake_template_parity.py --scope=opencode-adapter` and assert exit 0.
4. `test_us0126_test_marker_checklist` (AC-4) — grep `tests/` for `test_us0121_*`..`test_us0125_*` markers (aggregate per-story checklist).
5. `test_us0126_readme_no_dec_leak` (AC-5a) — US-0071 sanitization grep on `README.md` + `template/its_magic/README.md`: no `DEC-xxxx` ids in operator-facing sentences.
6. `test_us0126_runbook_no_dec_leak` (AC-5b) — US-0071 sanitization grep on runbook US-0126 section + template: no `DEC-xxxx` ids in operator-facing sentences (cross-references allowed only in Boundaries/evidence subsection).
7. `test_us0126_program_dod_documented` (AC-6) — grep runbook for DoD sentence key phrases: "fresh `its-magic --host opencode` install", "distinct sessions", "refuse writes on non-zero exit".
8. `test_us0126_default_host_reminder` (AC-7) — grep runbook + README for: "Default install is cursor-only", "`--host opencode`", "`--host both`".
9. `test_us0126_out_of_scope_listed` (AC-8) — grep runbook + README for each excluded item: "standalone runtime", "OpenCode fork", "VS Code contrib rewrite", "Caveman", "Cursor browser as primary UAT".
10. `test_us0126_template_doc_parity` (AC-9) — assert `installer-owned-paths.manifest` active↔template byte-identical (no new entries) + `docs/engineering/runbook.md` active↔template byte-identical.
11. `test_us0126_cursor_docs_not_deleted` (AC-10) — **deterministic static check**: assert `.cursor/commands/` and `.cursor/agents/` directories still exist with expected file names vs current kit inventory (a sorted file-name list checked into the repo at execute time). NOT a frozen pre-US-0126 git snapshot (fragile). NOT a hash manifest of the entire `.cursor/` directory (over-broad). No git history dependency; no live OpenCode probe.
12. `test_us0126_prior_story_markers_present` (AC-4 aggregate) — grep `tests/` for `test_us0121_*`..`test_us0125_*` markers (aggregate prior-story marker presence — kept separate from marker 4 for explicit defense in depth; may be merged with marker 4 at execute if redundancy is justified, but architecture locks 12 for clarity).

Surjective AC coverage: AC-1 (marker 1), AC-2 (marker 2), AC-3 (marker 3 + marker 10), AC-4 (markers 4, 12), AC-5 (markers 5, 6), AC-6 (marker 7), AC-7 (marker 8), AC-8 (marker 9), AC-9 (marker 10), AC-10 (marker 11). Every AC has ≥1 marker.

**Rejected alternatives**: (a) Collapse to 11 markers by merging marker 4 + marker 12 — rejected as AC-5 split is real (readme vs runbook no-dec-leak are distinct surfaces); keeping 12 preserves one-test-per-AC clarity; (b) Frozen pre-US-0126 git snapshot for AC-10 — rejected as fragile (requires immutable historical git state); (c) Live OpenCode probe — rejected as vision D10 violation.

### 6. Template parity manifest = unchanged (DQ8 LOCKED — AC-9)

`docs/engineering/context/installer-owned-paths.manifest` (active + template byte-identical) is **unchanged** — no new entries. The runbook is already installer-owned via `docs` in `[install_include_paths]`; the new "OpenCode host operator runbook (US-0126)" h2 section is part of the runbook — no new manifest entry needed. `tests/us0126_contract_test.py` is NOT installer-shipped (matches US-0121..US-0125 pattern); it is parity-validated via the new `OPENCODE_ADAPTER_PAIRS` pair (DQ3 lock). The manifest itself stays byte-identical active↔template; the runbook active↔template parity is validated via the new `OPENCODE_ADAPTER_PAIRS` runbook pair (DQ3 lock).

**Rejected alternatives**: (a) Add `tests/us0126_contract_test.py` to `[install_include_paths]` — rejected as it breaks the US-0121..US-0125 pattern (test files are NOT installer-shipped; parity-validated via `OPENCODE_ADAPTER_PAIRS` only); (b) New `[opencode_us0126_install_include_paths]` section — rejected as over-engineered; the existing `[opencode_install_include_paths]` section already covers the epic's installer-shipped surfaces; US-0126 adds no new installer-shipped file.

### 7. Compose, do not amend (US-0121/US-0124/US-0125 h2 sections untouched)

US-0126 owns the consolidated cross-host reason-code table and the new operator-runbook h2 section. The US-0121 installer-flag h2 (`## OpenCode host mode (US-0121)`), the US-0124 stub reason-code h2 (`## OpenCode orchestrator plugin reason codes (US-0124)`), and the US-0125 stub reason-code h2 (`## OpenCode thin commands + validator bridge (US-0125)`) are NOT edited by US-0126 — US-0126 cross-links to them. The README user-visible OpenCode host blurb is additive (no existing README content rewritten). No Cursor kit docs are deleted (AC-10). US-0071 sanitization enforced (no DEC ids in operator prose; cross-references to DEC ids allowed only in the Boundaries subsection, not in operator prose).

### 8. Isolation + runtime proof (US-0048 / DEC-0029 / DEC-0038)

- `phase_id=architecture`, `role=tech-lead`, `story_id=US-0126`, `sprint_id=(pending)`, `orchestrator_run_id=auto-20260825-01`
- `delivery_mode=ultra_lean`, `macro_phase=plan`, `model_id=glm-5.2-high` (CROSS_MODEL_REVIEW=1 — required)
- `fresh_context_marker=tl-US0126-architecture-20260825T160542Z-fresh`, `timestamp=2026-08-25T16:05:42Z` (UTC)
- `runtime_proof_id=rp-auto-20260825-01-architecture-tech-lead-20260825T160542Z-US-0126`
- `proof_hash=EEE667DAEE41839D9695C25D4BBFF2D8FA383CAEF6FDA69BFFEAF1D28B5263A2` (SHA-256 of sorted-key compact JSON payload, UTF-8 bytes via `C:\Users\flow\AppData\Local\Programs\Python\Python312\python.exe` hashlib; independently recomputed and confirmed match)
- `proof_ttl_seconds=3600`, `proof_ttl=2026-08-25T17:05:42Z` (UTC = issued_at + 3600s)
- Prior proof consumed: `rp-auto-20260825-01-research-tech-lead-20260825T155615Z-US-0126` (hash `22035314D2CD5763ECDBED6A3426B696A57331035F84E3BDEC97FC7DFAC3B188`, ttl 2026-08-25T16:56:15Z — consumed before RUNTIME_PROOF_STALE).

## Consequences

- **Positive**: Operators on the OpenCode host get a single consolidated cross-host reason-code table + a locked operator runbook section (program DoD + default-host reminder + out-of-scope list) mirrored active↔template byte-identical; `--scope=opencode-adapter` validates the whole epic surface (10 pairs) in one invocation; 12 static/grep contract markers prove AC-1..AC-10 without any live OpenCode runtime probe (vision D10 upheld); US-0121/US-0122/US-0123/US-0124/US-0125 stub h2 sections stay untouched (compose, do not amend); US-0071 sanitization enforced (no DEC ids in operator prose); AC-10 cursor-kit-not-deleted enforced via deterministic static check (no fragile git snapshot).
- **Negative**: One new runbook h2 section (mirrored); one README blurb (mirrored); one `OPENCODE_ADAPTER_PAIRS` additive extension (2 pairs); one new contract test file (12 markers, mirrored).
- **Neutral**: `installer-owned-paths.manifest` unchanged (runbook already covered by `docs`); `tests/us0126_contract_test.py` not installer-shipped (matches US-0121..US-0125 pattern); Cursor `.cursor/commands/*.md` + `.cursor/agents/*.md` unchanged; US-0102 volatile-ID rule respected.

## Risks

All 6 risks from R-0109 US-0126 ACCEPTED (R1 reason-code namespace collision, R2 parity-scope drift, R3 operator-sentence DEC leakage, R4 template-parity gap, R5 program-DoD ambiguity, R6 cursor-kit deletion temptation). Mitigations: DQ2 consolidated table + no wrapper (R1); DQ3 additive `OPENCODE_ADAPTER_PAIRS` + layer split (R2); DQ6/DQ7 US-0071 sanitization (R3); DQ8 runbook pair in `OPENCODE_ADAPTER_PAIRS` (R4); DQ5 operationally precise locked wording (R5); DQ4 marker 11 deterministic static check (R6). Three research critic NBs closed (C1 parity grep false-pass, C2 runbook whole-file pair coupling, C3 marker count + AC-10 baseline).

## References

- `docs/engineering/architecture.md # US-0126` (companion architecture section)
- `docs/engineering/research.md ## R-0109 ### Deepened findings — US-0126` (DQ1..DQ8 LOCKED)
- `docs/product/backlog.md ## US-0126` (10 ACs + D1..D10 + DQ1..DQ8)
- `decisions/DEC-0125.md` (US-0125 thin commands + validator bridge — DQ7 raw Python reason codes + `OPENCODE_VALIDATOR_FAILED` wrapper REJECTED; read-only compose)
- `decisions/DEC-0124.md` (US-0124 orchestrator plugin — DQ4 reason-code namespace + DQ6 `OPENCODE_DRIVER_INVOKE_FAILED`; read-only compose)
- `decisions/DEC-0122.md` (US-0122 seven role agents — read-only compose)
- `decisions/DEC-0120.md` (US-0121 host default cursor-only + installer `--host` flag docs hook; read-only compose)
- `decisions/DEC-0060.md` (US-0078 `intake_evidence_validate.py` persistence gate — read-only compose)
- `decisions/DEC-0051.md` (US-0069 phase→role matrix — read-only compose)
- https://opencode.ai/docs/commands/ (web — markdown command files; already consulted for US-0125)
- https://opencode.ai/docs/agents/ (web — primary/subagent modes; already consulted for US-0122)
- https://opencode.ai/docs/cli/ (web — `opencode run` non-interactive CLI; already consulted for US-0124)
- https://opencode.ai/docs/providers/ (web — `/connect` multi-provider keys; already consulted for US-0123)

