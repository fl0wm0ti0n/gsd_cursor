# DEC-0125: Thin OpenCode commands + Python validator bridge — 15-file curated subset + dispatch-only bodies + clone guard + validator→artifact mapping + defense-in-depth + `/auto` dispatch-only (US-0125)

- Date: 2026-08-24
- Status: Accepted
- Story: `US-0125`
- Research refs: `R-0109` (deepened 2026-08-24, tech-lead, `/research`, auto-20260824-02 — DQ1..DQ8 LOCKED for US-0125; US-0121 Q1..Q12 + US-0122 DQ1..DQ8 + US-0123 DQ1..DQ10 + US-0124 DQ1..DQ8 locks preserved)
- Composes on: **`US-0001`** (phase *names* + artifact outputs compose; do NOT triple-copy command bodies — AC-9), **`US-0078` / `DEC-0060`** (`intake_evidence_validate.py` remains the persistence-blocking gate; thin commands subprocess, do not reimplement — AC-3, AC-6), **`US-0121` / `DEC-0120`** (host default cursor-only; commands live in reserved `template/.opencode/commands/` slot — `.gitkeep` replaced by 15 files), **`US-0122` / `DEC-0122`** (seven role agents — commands bind via `agent: <role>`; agents unchanged), **`US-0124` / `DEC-0124`** (plugin owns spawn + `ctx.tool.hook` enforcement; `/auto` is dispatch-only; missing command must not disable plugin — US-0124 AC-7 ↔ US-0125 AC-7; plugin must not own command bodies), **`US-0126`** (owns full runbook + reason-code table + `--scope=opencode-adapter` parity; US-0125 ships stub reason-code reference only), **`US-0102` / `DEC-0087`** (no vendor slugs in `template/` — no `model:` literals in any command frontmatter)
- Extends: `template/.opencode/commands/` (US-0121 reserved slot — `.gitkeep` replaced by 15 dispatch-only command files); `tests/us0124/mock_ctx.ts` (US-0124 harness — extended with `mockSubprocess` for US-0125 contract tests; additive, US-0124 surface unchanged)
- Related: `US-0121` (pack path + reserved `template/.opencode/commands/` slot — consumed), `US-0122` (role agents — commands bind via `agent:` frontmatter; unchanged), `US-0123` (per-role model slug routing — commands omit `model:`; OpenCode host resolves role→slug via US-0123 catalog), `US-0124` (orchestrator plugin — `/auto` command dispatches to `auto` agent; plugin owns spawn + `ctx.tool.hook` enforcement; US-0125 authors validator→artifact mapping that the plugin consumes), `US-0126` (full runbook + reason-code table — US-0125 ships stub reason-code reference only)
- Supersedes: none

## Context

US-0125 is the fifth slice of the six-story OpenCode adapter epic (US-0121..US-0126). US-0121 shipped the empty-but-valid `template/.opencode/` pack + the `--host` installer switch. US-0122 populated the pack with eight markdown role agents and locked the Layer-1 permission matrix. US-0123 locked the per-role `provider/slug` resolution chain. US-0124 shipped the orchestrator plugin that makes `/auto` spawn-only on the OpenCode host. US-0125 owns **Layer 3** — the named slash-command entry points as **dispatch-only** markdown files at `template/.opencode/commands/<name>.md`, plus the **Python validator bridge contract** that keeps `scripts/*_validate.py` the single source of truth for persistence-blocking gates.

The commands **are** dispatch-only (do **not** clone Cursor 200-line command bodies per AC-1/AC-9). Success test (b) lives here: a model that ignores its prompt still cannot run `/release` (or any release persistence path) after a failing validator — the US-0124 plugin's `ctx.tool.hook("execute.before")` is the enforcement layer that a prompt-ignoring model cannot bypass (DQ4 defense in depth). The command prose is the *invitation* (diagnostics); the plugin is the *enforcement* (persistence).

R-0109 DQ1..DQ8 deepened the eight discovery questions against authoritative OpenCode command docs (`https://opencode.ai/docs/commands/`) and agent docs (`https://opencode.ai/docs/agents/`). This DEC locks the answers as the cross-cutting thin-commands + validator-bridge contract that US-0126 (full runbook) inherits without re-deriving.

Three sovereign-critic non-blocking carry-forwards from the research phase are closed here:

1. **`ik_us0125_dq5_auto_plugin_overlap`** — D2 listed `/auto` in the named inventory while DQ5 asked whether US-0125 ships a `/auto` command at all; failure mode = duplicate spawn paths. Closed by **DQ5** (US-0125 ships a `/auto` command file as a **dispatch-only** entry point — `agent: auto` + `subtask: false` + body says "invoke the orchestrator agent; the plugin owns spawn"; NO spawn logic in the command; the plugin (US-0124) remains the single spawn owner; defense in depth — the command is the slash-entry, the agent is the prompt layer, the plugin is the enforcement + spawn layer).
2. **`ik_us0125_dq3_validator_scope_boundary`** — D4 named `intake_evidence_validate.py` + `bug_issue_validate.py` + "other fail-closed CLIs" without a locked enumeration. Closed by **DQ3** (US-0125 in-scope validator set = the two named persistence-blocking gates PLUS a documented *bridge contract* (subprocess + fail-closed + raw Python reason code) that any kit validator can invoke through; US-0125 does NOT enumerate every kit validator — the bridge is the contract, not the list; US-0126 owns the full validator enumeration in the runbook).
3. **`ik_us0125_spec_scope_minimal_pass`** — informational; spec did not over-scope. Closed without architecture change. US-0125 ships the minimum commands + bridge contract + stub-harness required to satisfy AC-1..AC-10; US-0126 owns the full-runbook surface.

A fourth carry-forward from the architecture prompt itself is closed here:

4. **`ik_us0125_dq4_plugin_mapping_coupling`** — DQ4 locked that US-0125 owns the validator→artifact mapping table and US-0124 owns the plugin hook that reads it. Closed by specifying the mapping as **additive data** authored by US-0125 and consumed read-only by the US-0124 plugin hook — additive compose, no spawn-owner change. The plugin hook logic is unchanged; only the mapping data is new.

Research basis: **`R-0109`** US-0125 deepened findings (DQ1..DQ8 LOCKED for `/architecture`; US-0121 Q1..Q12 + US-0122 DQ1..DQ8 + US-0123 DQ1..DQ10 + US-0124 DQ1..DQ8 locks preserved, not wiped).

## Decision

### 1. Command file inventory = curated 15-file subset at `template/.opencode/commands/<name>.md` (DQ1 LOCKED — AC-1, AC-9)

Ship 15 dispatch-only markdown command files (12 lifecycle phases + `/auto` + `/quick` + `/ask`):

- **Lifecycle (12)**: `intake.md`, `discovery.md`, `research.md`, `architecture.md`, `sprint-plan.md`, `plan-verify.md`, `execute.md`, `qa.md`, `verify-work.md`, `release.md`, `closure.md`, `refresh-context.md`.
- **Orchestrator dispatch (1)**: `auto.md` (dispatch-only — DQ5).
- **Delivery-mode entries (2)**: `quick.md` (`mega_quick` per US-0096 / DEC-0082), `ask.md` (read-only, agent-agnostic).

The 10 omitted cursor commands (`pause`, `resume`, `status-reconcile`, `memory-audit`, `milestone-start`, `milestone-complete`, `phase-context`, `map-codebase`, `security-review`, `sovereign-critic`) are NOT shipped — their function is covered by the plugin (US-0124), the outer driver, or built-in `@explore`/`@scout` subagents + `/ask`. `/resume` is intentionally omitted because OpenCode session continuation (`--continue`/`--session`/`--fork` per `opencode run`) plus the outer driver's `resume_brief.md` covers the same surface without a slash command.

**Rejected alternatives**: (a) Full 1:1 mirror (25 files) — rejected as AC-1 violation at the intent level + unnecessary clone-guard surface; (b) Lifecycle-only (12 files) — rejected as it omits `/auto` (required for OpenCode `/auto` to exist as a slash command per DQ5) and `/quick` (required for `mega_quick` delivery-mode parity).

### 2. Clone guard = per-file line cap ≤ 20 + normalized-text similarity ≤ 0.30 (DQ2 LOCKED — AC-2)

Two metrics, defense in depth:

- **Per-file line cap**: ≤ **20 lines** (including frontmatter + body). Files > 20 lines fail the guard.
- **Normalized-text similarity threshold**: normalized token-set ratio vs `.cursor/commands/<name>.md` ≤ **0.30**. Normalization: strip frontmatter + lowercase + strip punctuation + strip the shared phase-name vocabulary. Use stdlib `difflib.SequenceMatcher` (no new test dependency). Files with similarity > 0.30 fail the guard.

`test_us0125_clone_guard` iterates over the 15 shipped `.opencode/commands/*.md` files; for each, asserts (i) line count ≤ 20, (ii) normalized similarity vs `.cursor/commands/<name>.md` ≤ 0.30. Fails on either violation.

**Rejected alternatives**: (a) Per-file byte/line cap only — rejected as a clone in spirit could still copy distinctive prose while staying under N lines; (b) Normalized-text similarity only — rejected as it may false-positive on legitimate shared vocabulary (phase names, artifact paths).

### 3. Validator bridge contract = two named CLIs + generic bridge (DQ3, DQ4, DQ7 LOCKED — AC-3, AC-5)

**In-scope named persistence-blocking gates** (US-0125 ships explicit subprocess bridge + contract tests):

- `scripts/intake_evidence_validate.py` — `python scripts/intake_evidence_validate.py --repo . [--enforce]` → exit 0 = pass, exit non-zero = fail (raw Python reason code on stderr, e.g. `INTAKE_PERSISTENCE_BLOCKED`, `INTAKE_REQUIRED_TOPIC_MISSING`).
- `scripts/bug_issue_validate.py` — `python scripts/bug_issue_validate.py --repo . --check-acceptance` → exit 0 = pass, exit non-zero = fail (raw Python reason code on stderr, e.g. `BUG_ISSUE_VALIDATION_FAILED`).

**Generic bridge contract** (US-0125 documents; any kit validator can use it): `python scripts/<validator>.py --repo . [--enforce] [--scope <scope>]` → exit 0 = pass, exit non-zero = fail (raw Python reason code on stderr). The plugin/command subprocess invokes this and on non-zero exit emits the raw Python reason code (DQ7) and refuses the persistence path (DQ4).

**Out-of-scope** (US-0126 owns the full enumeration in the runbook): `closure-verification`, `enforce-triad-hot-surface`, `model_tier_validate`, `release_changelog_lib`, `check_intake_template_parity`, `sovereign_critic_validate`, `sovereign_loop_validate`, `validate_autonomy_stop_matrix`, `validate_readme_feature_coverage`, etc. These remain Python SOT; US-0125's bridge contract *applies* to them but US-0125 does not enumerate them.

**Rejected alternatives**: (a) Two named CLIs only — rejected as it would force US-0126 to re-author the bridge for every additional validator; (b) Enumerate every kit validator — rejected as it violates AC-3 (US-0125 owns the *bridge contract*, not the validator inventory) and pre-empts US-0126 runbook territory.

### 4. Defense-in-depth validator enforcement (DQ4 LOCKED — AC-3, AC-4)

Two layers, independent:

- **Command prose** (`.opencode/commands/<phase>.md` body): a short line says "Before writing to `<artifact>`, run `python scripts/<validator>.py --repo .` and surface any non-zero exit reason code to the operator. The orchestrator plugin enforces persistence." This is *informational* — it tells the agent the right thing to do, but does not own enforcement.
- **Plugin enforcement** (US-0124 `template/.opencode/plugins/orchestrator.ts` `ctx.tool.hook("execute.before")`): on any `edit`/`write`/`apply_patch` to a persistence-blocking artifact path, the plugin subprocesses the corresponding validator and refuses the write on non-zero exit, emitting the raw Python reason code (DQ7). This is the *enforcement* layer that AC-4 success test (b) asserts.

**Boundary with US-0124** (critic NB `ik_us0125_dq4_plugin_mapping_coupling` closed): US-0124 owns the plugin `ctx.tool.hook` enforcement; US-0125 owns the command prose + the *validator→artifact mapping* (which validator gates which artifact path). The mapping is **additive data** authored by US-0125 and consumed read-only by the US-0124 plugin hook — additive compose, no spawn-owner change. The plugin hook logic is unchanged; only the mapping data is new.

**Validator→artifact mapping table** (US-0125-owned, US-0124-consumed):

| Artifact path (persistence-blocking) | Validator CLI | Reason code surface |
|----------------------------------------|---------------|---------------------|
| `handoffs/intake_evidence/*.json` (intake evidence writes) | `scripts/intake_evidence_validate.py --repo . --enforce` | `INTAKE_PERSISTENCE_BLOCKED`, `INTAKE_REQUIRED_TOPIC_MISSING`, ... |
| `docs/product/backlog.md` bug rows + `docs/product/acceptance.md` bug rows | `scripts/bug_issue_validate.py --repo . --check-acceptance` | `BUG_ISSUE_VALIDATION_FAILED`, ... |
| (other persistence-blocking artifacts) | (generic bridge contract — US-0126 owns enumeration) | (raw Python reason code per validator) |

**Rejected alternatives**: (a) Command prose owns subprocess enforcement — rejected as a prompt-ignoring model can skip the subprocess and write anyway; AC-4 success test (b) cannot be enforced at the command-prose layer; (b) Plugin owns everything (no command prose) — rejected as it loses the diagnostics invitation (the agent would not know to run the validator for diagnostics before the hook fires).

### 5. `/auto` dispatch-only entry (DQ5 LOCKED — AC-1, AC-7; critic NB `ik_us0125_dq5_auto_plugin_overlap` closed)

`template/.opencode/commands/auto.md` is a **dispatch-only** entry point:

- Frontmatter: `description: "its-magic auto: orchestrator dispatch entry (spawn-only)."` + `agent: auto` + `subtask: false` (the `auto` agent is `mode: primary` — `/auto` runs in the primary session, not as a subagent).
- Body: a short dispatch prose that names the orchestrator role + points to the plugin for spawn + STOP. No spawn logic, no `ctx.session.create` call, no state-machine prose.
- The command binds to the `auto` agent (US-0122 `template/.opencode/agents/auto.md` — `mode: primary`, `edit: deny`, `bash: deny`, `task` 7-role allow-list). The agent's permission array is the first enforcement layer; the plugin's `ctx.tool.hook` + `ctx.session.create` is the second.
- `test_us0125_auto_command_dispatch_only` asserts (i) `auto.md` line count ≤ 20 (DQ2), (ii) `auto.md` has no `ctx.session.create` / `Session.create` / `spawn` logic literals, (iii) `auto.md` `agent: auto` frontmatter is present.
- **Missing `/auto` (AC-7)**: if `auto.md` is deleted/renamed, the operator can still invoke the orchestrator agent via `@auto` mention (US-0122 agent is independent of the command file) and the plugin still loads via `.opencode/plugins/` auto-discovery. `test_us0125_missing_command_does_not_disable_plugin` asserts this.

**Rejected alternatives**: (a) No `/auto` command file — rejected as OpenCode slash commands are markdown files; without `auto.md`, the operator has no `/auto` slash entry; (b) `/auto` command file with spawn logic — rejected as it violates US-0124 DQ8 (plugin owns spawn) + AC-1 (no 200-line clones).

### 6. Frontmatter shape (DQ6 LOCKED — AC-1)

Per OpenCode command docs (`https://opencode.ai/docs/commands/`):

- **Lifecycle phase commands** (12 files): frontmatter = `description: "its-magic <phase>: <one-line role summary>."` + `agent: <role>` where role is the US-0069 / DEC-0051 phase→role default (intake→po, discovery→po, research→tech-lead, architecture→tech-lead, sprint-plan→tech-lead, plan-verify→qa, execute→dev, qa→qa, verify-work→qa, release→release, closure→qa with prompt `role=qe`, refresh-context→curator). No `model:`, no `subtask:`.
- **`/auto`**: frontmatter = `description: "its-magic auto: orchestrator dispatch entry (spawn-only)."` + `agent: auto` + `subtask: false`. No `model:`.
- **`/quick`**: frontmatter = `description: "its-magic quick: ultra-lean single-phase path (mega_quick delivery mode)."` + `agent: tech-lead` (quick is a tech-lead-owned phase per US-0096 / DEC-0082). No `model:`, no `subtask:`.
- **`/ask`**: frontmatter = `description: "its-magic ask: answer questions using project context, read-only."` + no `agent:` (defaults to current agent — `/ask` is agent-agnostic). No `model:`, no `subtask:`.
- Body: a short dispatch prose (≤ ~12 lines) naming the phase_id + artifact path list + STOP. No `$ARGUMENTS`, no shell injection, no `@file` inclusion.

**Closure `agent:` rule** (no `qe.md` in pack): the seven role agents shipped by US-0122 are `po`, `tech-lead`, `dev`, `qa`, `release`, `curator`, `security`, plus `auto` (orchestrator). There is **no `qe.md` agent**. The `/closure` command binds `agent: qa` with prompt `role=qe` — same as Cursor Task type `qa` + `role=qe` per DEC-0051 / US-0120. The agent is `qa`; the prompt role is `qe`. This is the documented closure-phase binding for the OpenCode host.

**Rejected alternatives**: (a) Set `model:` in template commands — rejected as it violates US-0102 (no vendor slugs in `template/`) + US-0123 (model routing owned by catalog); (b) Set `subtask: true` on lifecycle phase commands — rejected as the agent's own `mode: subagent` from US-0122 handles subagent invocation; (c) Ship a `qe.md` agent — rejected as out of scope for US-0125 (US-0122 owns agent inventory; closure binds `qa` + `role=qe`).

### 7. Reason-code boundary (DQ7 LOCKED — AC-5)

- **Validator non-zero exit** (the validator ran and returned non-zero): surface the **raw Python reason code** from stderr. No `OPENCODE_*` wrapper. Examples: `INTAKE_PERSISTENCE_BLOCKED`, `INTAKE_REQUIRED_TOPIC_MISSING`, `BUG_ISSUE_VALIDATION_FAILED`.
- **Subprocess invocation failure** (the Python CLI could not be invoked — missing Python, missing script, subprocess timeout): emit `OPENCODE_DRIVER_INVOKE_FAILED` (already locked by DEC-0124 DQ6). This is the *host-specific* code for "the bridge itself broke" — distinct from the validator's own non-zero exit.
- **No silent skip** (AC-5): both failure modes emit a reason code and refuse the persistence path. The plugin's `ctx.tool.hook("execute.before")` is the enforcement layer (DQ4); the command prose surfaces the code to the operator for diagnostics.
- **Reason-code table location**: US-0125 ships a **stub reason-code reference** in the US-0125 runbook section of `docs/engineering/runbook.md` (h2 anchor `## OpenCode thin commands + validator bridge (US-0125)`) that lists the two named validator CLIs + their canonical Python reason codes + a cross-link to US-0126 for the full reason-code table. US-0126 owns the full table; US-0125 ships the stub only — no duplication of remediation text.

**Rejected alternatives**: (a) `OPENCODE_VALIDATOR_FAILED: <python_code>` wrapper — rejected as it duplicates the reason-code namespace (every Python code now has two surface forms) and pre-empts US-0126's reason-code table; (b) Silent skip on subprocess invocation failure — rejected as it violates AC-5 (no silent skip).

### 8. Mock-ctx + mock-subprocess harness (DQ8 LOCKED — AC-4, AC-8, AC-10)

Extend the US-0124 `MockCtx` harness (`tests/us0124/mock_ctx.ts`) with a `mockSubprocess` field (or add a sibling `tests/us0125/mock_subprocess.ts` imported by the US-0125 test). The mock subprocess accepts a scripted `nextExitCode` (0 or non-zero) + `nextStderr` (the raw Python reason code) + `nextThrow` (for `OPENCODE_DRIVER_INVOKE_FAILED` simulation). The plugin's `ctx.tool.hook("execute.before")` calls the mock subprocess; tests assert the hook refuses the write on non-zero. No OpenCode runtime dependency — CI runs pure Node/Bun (same as US-0124). **Runner: Node** (consistent with US-0124 DQ3).

`test_us0125_release_blocked_after_failing_validator` (success test (b)):

1. Load `template/.opencode/plugins/orchestrator.ts` via dynamic import, call `setup(mockCtx)`.
2. Program `mockCtx.subprocess` to return exit code 1 + stderr `INTAKE_PERSISTENCE_BLOCKED` for any `python scripts/intake_evidence_validate.py` invocation.
3. Drive the plugin's `ctx.tool.hook("execute.before")` with a synthetic `edit` tool call to a release-persistence artifact path (e.g. `handoffs/release_queue.md`).
4. Assert the hook (i) calls `mockCtx.subprocess` with the expected validator argv, (ii) refuses the write (returns a deny action or throws), (iii) emits the raw Python reason code `INTAKE_PERSISTENCE_BLOCKED` (not a wrapper).
5. Repeat with `mockCtx.subprocess` programmed to throw (simulating subprocess invocation failure) → assert the hook emits `OPENCODE_DRIVER_INVOKE_FAILED` and refuses the write.

**Rejected alternatives**: (a) Live OpenCode probe — rejected as it adds OpenCode runtime dependency to CI (flaky, version-coupled, slow); forbidden by AC-10 / vision D10; (b) Static AST/grep only — rejected as it cannot assert runtime behavior; AC-4 success test (b) needs the mock to return non-zero and assert the write is refused.

## Consequences

- **Positive**: Operators on the OpenCode host get named slash-command entry points for the 12 lifecycle phases + `/auto` + `/quick` + `/ask` without 200-line Cursor clones; success test (b) is provable via mock-ctx+mock-subprocess harness + plugin `ctx.tool.hook("execute.before")` enforcement; Python validators remain the single source of truth (no TypeScript reimplementation); US-0126 inherits the locked command inventory + clone-guard + validator-bridge contract + defense-in-depth + `/auto` dispatch-only + frontmatter shape + reason-code boundary via this DEC without re-deriving; US-0001/US-0078/US-0121/US-0122/US-0124/US-0102 compose unchanged.
- **Negative**: 15 new template command files; one mock-subprocess harness extension; one new contract test file (11 markers); one stub runbook h2 one-liner; installer manifest rows for 15 command files.
- **Neutral**: US-0121 reserved `template/.opencode/commands/` slot consumed (`.gitkeep` replaced); US-0122 agents unchanged; US-0124 plugin unchanged (US-0125 authors mapping data, not plugin code); US-0102 volatile-ID rule respected; Cursor `.cursor/commands/*.md` unchanged.

## Risks

All 6 risks from R-0109 US-0125 ACCEPTED (R1 clone drift, R2 validator reimplementation temptation, R3 `/auto` spawn duplication, R4 reason-code namespace duplication, R5 missing command disables plugin, R6 live OpenCode probe in CI). Mitigations: DQ2 clone guard (R1), DQ4 defense-in-depth + AC-6 grep (R2), DQ5 dispatch-only `/auto` (R3), DQ7 raw Python codes + `OPENCODE_DRIVER_INVOKE_FAILED` (R4), DQ5 + AC-7 (R5), DQ8 mock-ctx+mock-subprocess harness (R6).

## References

- `docs/engineering/architecture.md # US-0125` (companion architecture section)
- `docs/engineering/research.md ## R-0109 ### Deepened findings — US-0125` (DQ1..DQ8 LOCKED)
- `docs/product/backlog.md ## US-0125` (10 ACs + D1..D10 + DQ1..DQ8)
- `decisions/DEC-0124.md` (US-0124 orchestrator plugin — DQ6 subprocess + DQ8 agent/plugin boundary; read-only compose)
- `decisions/DEC-0122.md` (US-0122 seven role agents — read-only compose)
- `decisions/DEC-0120.md` (US-0121 host default cursor-only + reserved `template/.opencode/commands/` slot)
- `decisions/DEC-0060.md` (US-0078 `intake_evidence_validate.py` persistence gate — read-only compose)
- `decisions/DEC-0051.md` (US-0069 phase→role matrix — read-only compose)
- https://opencode.ai/docs/commands/ (web — markdown command files + frontmatter `description`/`agent`/`model`/`subtask` + `$ARGUMENTS` + `!`backtick` + `@file` + subtask semantics)
- https://opencode.ai/docs/agents/ (web — primary/subagent modes + permission keys + `task` allow-list + `hidden` + built-in agents)

